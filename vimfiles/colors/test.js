window.addEvent('domready', function(){
   var name = function() {
        var container = new Element('div', { id: 'side-container' });
        container.adopt(document.getElements('.slide'))
   }
    if (a == 3) {
        break;
    }
});
